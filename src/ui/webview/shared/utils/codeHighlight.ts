/**
 * Ato syntax highlighting utilities.
 * TODO: Implement proper syntax highlighting for .ato code
 */

/**
 * Highlight ato code for display in code blocks.
 * Currently returns the code as-is; syntax highlighting can be added later.
 */
export function highlightAtoCode(code: string): string {
  return code
}
