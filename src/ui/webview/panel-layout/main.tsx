import { render } from "../shared/render";

function App() {
  return (
    <div className="panel">
      <h2>Layout</h2>
      <p>Select a target and build to preview the PCB layout.</p>
    </div>
  );
}

render(App);
