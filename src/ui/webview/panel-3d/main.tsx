import { render } from "../shared/render";

function App() {
  return (
    <div className="panel">
      <h2>3D Model</h2>
      <p>Select a target and build to preview the 3D model.</p>
    </div>
  );
}

render(App);
