# Webviews (Wrapper Only)

This directory is reserved for minimal VS Code webview wrappers.
The React UI server lives in `src/ui-server` and is loaded via iframe
in dev mode (http://localhost:5173).

Production packaging for UI assets is handled separately.
