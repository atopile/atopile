{% for libpart in libparts -%}
(libpart (lib "{{ libpart.lib }}") (part "{{ libpart.part }}")
  (description "{{ libpart.description }}")
  (docs "{{ libpart.docs }}")
  (footprints {% for footprint in libpart.footprints %}
    (fp "{{ footprint }}"){% endfor %})
  (fields {% for field in libpart.fields %}
    (field (name "{{ field }}") "{{ libpart.fields[field] }}")){% endfor %}
  (pins {% for pin in libpart.pins %}
    (pin (num "{{ pin.num }}") (name "{{ pin.name }}") (type "{{ pin.type }}")){% endfor %}){% if not loop.last %}
{% endif %}{%- endfor %})