(export (version "E")
  (design
    (source "{{ source }}")
    (date "{{ date }}")
    (tool "{{ tool }}")
  (components {% filter indent(width=4) %}
{{ components }}){% endfilter %}
(libparts {% filter indent(width=4) %}
{{ libparts }}){% endfilter %}
  (nets {% filter indent(width=4) %}
{{ nets }}){% endfilter %}))