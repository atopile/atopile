{
  "name": "atopile-playground-web",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "build": "esbuild src/main.ts --bundle --minify --format=iife --target=es2020 --outfile=../playground_app/static/landing.js && esbuild src/dashboard.ts --bundle --minify --format=iife --target=es2020 --outfile=../playground_app/static/dashboard.js"
  },
  "devDependencies": {
    "esbuild": "^0.25.9",
    "typescript": "^5.8.3"
  }
}
