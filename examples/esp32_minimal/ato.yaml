requires-atopile: ^0.9.0

paths:
  src: .
  layout: ./layouts

builds:
  esp32_minimal:
    entry: esp32_minimal.ato:ESP32_MINIMAL
    hide_designators: true
dependencies:
  - type: registry
    identifier: atopile/buttons
  - type: registry
    identifier: atopile/ti-tlv75901
  - type: registry
    identifier: atopile/usb-connectors
    release: 0.2.2
  - type: registry
    identifier: atopile/esp32

