requires-atopile: ^0.7.0

paths:
  src: ./
  layout: ./layout

builds:
  default:
    entry: pick_parts.ato:App
# dependencies:
#   - type: registry
#     identifier: atopile/buttons
#     release: 0.2.4
