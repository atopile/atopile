requires-atopile: ^0.10.0

paths:
  src: ./
  layout: ./layout

builds:
  top:
    entry: layout_reuse.ato:Top
    exclude_checks: ["PCB.requires_drc_check"]

  sub:
    entry: layout_reuse.ato:Sub
    exclude_checks: ["PCB.requires_drc_check"]
